# 第3-4阶段：规格建模 + 第8阶段：用例生成规范

> 本参考文件为 SDD 规格建模、TDD、Given/When/Then 规范、建模/核心维度、字段规范的**唯一权威细则来源**（原 ch9/10/13/15.4/15.5/20）。第3-4阶段与第8阶段读取本文件。Markdown 与 Excel 字段共同遵循（满足一致性）。

---

# 9、规格建模规范（SDD）

每个功能必须抽象为：Feature、Rule、Constraint、Input、Output、State、Exception、Acceptance Criteria。

必须建立 Given / When / Then 模型。

并建立：状态模型、数据模型、权限模型、错误模型、接口契约模型。

### 事实来源标注与开发校对（提升规格可信度·强制）

SDD 中以下三块标注「事实来源」，并由 @开发 校对（开发拥有实现层的权威信息，文档状态机/异常阈值/接口契约常与实现有差异）：

| SDD 要素 | 事实来源 | 校对要点 |
| -- | -- | -- |
| State（状态机） | 实现状态机 vs 需求文档状态机 | 实际状态定义与合法/非法流转，以实现为准；文档状态机差异处须在用例反映 |
| Exception（异常） | 超时阈值/重试次数/补偿动作 | 实现细节，须由开发提供真实阈值/次数/间隔/动作 |
| Input/Output（接口契约） | 接口名/入参/出参/类型 | 真实字段名/类型，喂第8阶段测试数据，闭合 0.4 杜撰禁令 |

* 信息源：§5【技术实现摘要】（@开发提供）优先；未提供时由澄清台账（角色路由，见 `references/clarification.md`）向 @开发 征集
* 校对结果同步更新"规则建模"section 与受影响用例 G/W/T，保持三者一致（与"重走第4阶段须以已落盘规则建模+用例G/W/T为重建依据"机制天然衔接）
* 与运行模式正交：完整模式 @开发 校对后才进第5阶段；连跑/轻量模式以需求文档为假设继续，标注"待@开发校对状态机/异常/契约"

### 规则建模 section 来源标注（破自证循环·闭合 0.9·强制）

> 规则建模 section 沉淀为 `TestCases_<需求标识>.md` 的 section（见 `references/output_write.md` 追溯性 section），并供 `verify_cases.py` rule_coverage 校验"每类规则被用例覆盖"。该 section 由模型自写，**若无来源约束会被 rule_coverage 反向洗白为"已覆盖"**（根因：自证循环）。强制每条规则项标注来源，破该循环：

* 每条规则项须标注来源：`[来源:需求文档<章节号/章节名>/台账Q<序号>/假设A<序号>]`（三选一或多）
* 来源=需求文档：规则直接来自需求某章节（最可信）
* 来源=台账Q<n>：规则来自澄清台账用户已答复项（权威事实）
* 来源=假设A<n>：规则来自已登记假设（连跑/轻量 P2/P3，须回显进待确认问题与假设清单）
* 无来源标记的规则项 -> 疑似脑补规则，`verify_cases.py` 检查15 规则来源软提示，selfcheck 检查15 阻断（补来源或转问题/假设后重跑）
* 用例关联规则列引用 `R<序号>`/`TP<序号>` 时，对应规则/风险/测试点须有来源标记，形成"用例->规则->来源"完整链路（闭合 0.9 业务行为来源追溯）

#### 第3阶段出口机器 gate（规则来源·写前·内存内·有界自修）

> **根因**：规则无来源标记本是第3阶段就能机器判定、却原设计要拖到写前第11检查15 / 写后第13 才反向捕获，缺陷回流滞后。本 gate 在第3阶段规则建模完、进第4阶段前就地把"脑补规则"拦住。

* **时机**：第3阶段规则建模完、进第4阶段前，内存内执行（零文件操作）
* **调用**：对规则建模 section 的 `lines` 调 `scripts/verify_cases.py` 的 `check_rule_source(lines)`（line-based，无需 keystone；第3阶段无用例表，不调 `run_inmemory`）
* **判定与自修**（≤2 轮）：
  * 无来源标记的规则项 → 内存内补 `[来源:需求文档<章节>/台账Q<序号>/假设A<序号>]` → 重跑
  * 确无来源（需求文档与台账均无对应规则）→ **硬阻断**，转为台账Q（P0/P1）/假设A（P2/P3）登记入【待确认问题与假设清单】，不静默保留
* **通过**→进第4阶段；此时规则建模 section 已通过来源校验
* **运行模式正交**：连跑/轻量 P2/P3 可转假设登记放行（沿用现有机制），P0/P1 硬阻断仍停

### 接口契约模型与变更影响清单（契约驱动分支·强制）

当输入含接口文档（Swagger/OpenAPI/接口说明）时启用契约驱动分支：把接口分解为结构化契约，圈定本次需求涉及的**变更接口**，沉淀"变更影响清单"section（用例表之前，与规则建模/风险清单同级）。

**入参结构化**：每个接口入参分解为 {字段名, 类型, 是否必填, 默认值, 枚举, 取值范围, 示例}（喂第8阶段测试数据 + 驱动 `references/methods.md` 统一接口测试矩阵）。

**变更影响清单 section**（强制沉淀格式）：

| 接口ID | 接口名/路径 | 方法 | 变更类型 | 变更描述 | 变更字段 | 受影响规则 | 受影响场景 | 风险等级 | 来源 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| API1 | /order/create | POST | 新增字段 | 新增 couponId 选填 | couponId:string,选填 | R5:满减、R6:券+积分叠加 | SC1:下单主流程 | P1 | [需求文档3.2] |

* 变更类型取值见 `config/validation_rules.json` change_types（新增接口/废弃接口/新增字段/删除字段/类型变更/必填变更/枚举变更/默认值变更/约束变更/行为变更）
* 每条标来源 `[来源:需求文档<章节>/台账Q<n>/假设A<n>/接口文档<版本>/接口文档diff 旧->新]`（破自证循环，复用检查15规则来源机制）
* 用例"关联规则"列引用 `API<序号>` 实现变更接口->用例精确追溯（与 R<序号>/TP<序号> 同机制）；引用 `SC<序号>` 实现场景->用例追溯
* 变更接口 = 风险清单"接口契约 P1"的具体化子集（见 `references/risk.md`）

**场景清单 section**（受影响场景追溯）：

| 场景ID | 场景描述 | 关联模块 |
| --- | --- | --- |
| SC1 | 下单->选商品->创建订单->支付 | 订单/支付 |

> 接口契约文档与变更影响清单为参数校验行为（必填缺失返回码、类型错传处理）的**合法来源**，与"需求文档/台账Q/假设A"并列（闭合 SKILL.md 0.4/0.9，见 `references/selfcheck.md` 检查15）。


---

# 10、测试驱动规范（TDD）

严格遵循 Red → Green → Refactor 原则。测试先于实现。

必须先定义：验收规格、测试规则、边界条件、异常条件、状态条件。

之后再设计：测试需求、测试模型、测试点、测试用例。

禁止跳过规格阶段直接输出测试用例。

---

# 13、Given / When / Then 规范

## 13.1 Given（前置条件）

必须明确：

### 用户状态
用户已登录 / 用户未登录 / 用户Token已过期

### 权限状态
普通用户 / 管理员 / 财务角色

### 数据状态
订单待支付 / 订单已支付 / 库存充足

### 系统状态
缓存存在 / 缓存不存在 / MQ正常

### 环境状态
第三方接口正常 / 第三方接口超时

## 13.2 When（操作步骤）

要求：一步一动作、明确测试数据、可自动化执行。

**When 列写入形态（强制）**：When 是 15 列用例表的**第 9 列单元格**，多步骤**单行**用 `；` 或 `步骤N：` 串联，**禁止**写成游离的编号列表（`1.\n2.\n3.`）、**禁止**单元格内含真实换行。游离编号列表会破坏 Markdown 管道表（每行须以 `|` 开头，单元格内真实换行会让续行被当成新行/截断表），致 `verify_md.py` 报"未找到表头行"、`gen_excel.py` 拒绝生成 .xlsx。

单元格内容示例（合规·单行串联）：
- `1.进入购物车点击提交订单；2.确认金额`
- `步骤1：打开订单创建页面；步骤2：输入商品编号A001；步骤3：输入购买数量100；步骤4：点击提交订单`

> 真实换行（`\n`）由 `scripts/gen_excel.py` 在 Excel 阶段按 `；`/`步骤N：` 边界注入（见 13.2 末换行符约定），.md 内不写真实换行。

### 步骤化子结构（强制·非可选优化）

When/Then 一律写入表格单元格，单元格内多步骤用 `；`/`步骤N：` 单行串联；**禁止**真实换行、**禁止**拆成列表/分步块/每条用例单独小节。复杂步骤可如下组织（仍为单元格内单行文本）：

```text
步骤1：动作 = 打开订单创建页面；步骤2：动作 = 输入商品编号A001，预期 = 商品信息回显；步骤3：动作 = 输入购买数量100，预期 = 金额计算正确；步骤4：动作 = 点击提交订单，预期 = 接口返回200，订单状态为待支付
```

在表格单元格中以"步骤N：动作；预期"分行串联，**保持一条用例一行、不拆行不合并单元格**（强制）。

> **换行符约定（Excel 自动换行关键）**：.md 单元格内多步骤可写成单行 `；`/`步骤N：` 串联（Markdown 表格单元格不能含真实换行）；Excel 生成时由 `scripts/gen_excel.py` 按本约定自动注入真实 `\n`(chr(10))，使 `Alignment(wrap_text=True)` 在步骤边界硬换行。**禁止**生成 Excel 后单元格内 When/Then 仍为 `；` 单行无换行——会导致即便 wrap_text 也只在列宽边界软换行、可读性差。多断言在同一步骤内用 `；` 串联不换行。

## 13.3 Then（预期结果）

必须覆盖：页面结果、接口结果、数据结果、状态结果、缓存结果、MQ结果、日志结果。根据场景按需选择。禁止过度断言。

> **多断言换行约定**：Then 含多条断言时，.md 内可用 `；` 串联；Excel 生成时由 `scripts/gen_excel.py` 按断言边界注入 `\n`(chr(10))，配合 `Alignment(wrap_text=True)` 在断言边界硬换行（与 13.2 换行符约定一致）。

### 断言完整性（#2·强制·防"测试通过却漏 bug"）

不完整 oracle（只断言响应、不断言副作用）是"测试通过却上线出 bug"的头号原因。状态变更类用例须**双重断言**：

* **状态变更类用例**（When 含 创建/支付/扣减/更新/删除/撤销/退款/发货 等会改变业务状态或数据的操作）的 Then **必须同时包含**：
  * 接口结果（HTTP 状态码/错误码/status 字段）——验证操作本身
  * 数据/状态结果（订单状态更新为已支付、库存数量减少N、余额变更）——验证副作用真实发生
* 仅断言"接口返回200"而不断言数据/状态副作用的用例判定不完整（检查13，见 `references/selfcheck.md`）
* `scripts/verify_cases.py` 软性提示：状态变更类用例 Then 仅含接口结果、缺数据/状态副作用

> 例：`When=创建订单`，错误 Then：`接口返回200`（缺库存扣减/订单状态断言）；正确 Then：`接口返回200且status=SUCCESS；订单主存储中订单状态为待支付；库存数量减少2`。

---

# 15.4 测试建模覆盖维度

必须建立：规则模型、状态模型、权限模型、数据模型、时序模型、接口契约模型、风险模型。

---

# 15.5 核心测试维度

按需覆盖：

### 输入类
字段不传（区分 key 不传 / Null 值 / 空串 三态）、类型不匹配（异型值传入）、空值、Null、特殊字符、非法字符。

### 边界类
最小值、最大值、临界值、边界内邻接值（min+1/max-1，刚好满足约束应通过）。4 值齐全，不得用临界覆盖边界内（见 `references/methods.md` 边界值 4 值模型）。

### 状态类
合法流转、非法流转、回滚。

### 权限类
未登录、Token失效、越权访问。

### 接口类
参数校验（拆为 存在性/类型/值域/组合 四子维）、返回结构、错误码、超时、熔断。

### 并发类
重复提交、并发更新、重复消费。

### UI类

> UI 测试锚定"界面结构是否正确承载业务规则/数据/权限/状态"，而非"元素存在/框架渲染"。判定线（与 SKILL.md 0.1 一致）：一条 UI 用例是否过度 = 看 Then 断言"业务可观测变化"还是"元素存在性"，前者属必测、后者属过度（框架类 UI 用例 Then 多为元素存在性，仍会被检查7 拦下）。测试类型填"功能"，测试维度填"界面验证"。

**1. 查询/筛选条件区**
* 默认查询态：进入页面默认条件（默认时间范围/筛选/排序/分页）符合业务预期，默认态返回数据集正确（非"全量"误当默认）
* 单条件查询：每条件单独生效、过滤结果正确、清空单条件回到上级态
* 组合查询：多条件 AND/OR、条件间互斥/依赖（选 A 则 B 禁用或必填）、级联清空、组合结果正确
* 重置清空：回到默认态（非空态）、不误清不可清项、清空后触发重新查询
* 模糊/精确与空串语义：通配符、空串=全量 or 不查、大小写、前后空格 trim
* 查询与分页排序联动：翻页/排序后条件保持、查询后跳回首页

**2. 列表/表格展示区**（最高价值）
* 字段展示完整性：齐全、与需求一致、无多余/缺失字段
* 字段映射正确性：状态码→状态文案、枚举→中文、ID→名称关联展示
* 字段格式与精度：金额精度/千分位/单位、日期时间格式与时区、长文本截断、null/空值占位符（"-"或空）一致
* 分页：首页/末页/上下页、每页条数、总数总页数、越界页/超大页码、翻页后条件与排序保持
* 排序：单列升降序、多列排序（若支持）、空值位置、排序稳定性
* **列表四态区分**：无数据态 / 无权限数据态（有权限无数据）/ 查询无结果态 / 加载失败态，提示文案与入口不同
* 行内/批量操作：行内按钮按权限/状态显隐（终态行无"编辑"）、批量全选/反选/跨页、批量上限、部分失败处理
* 列宽不破版：长字段不撑破、窄屏收起或横向滚动——仅断言"不破版且可读"，不断言像素

**3. 表单录入区**
* 必填标识与校验反馈：星号/标识、提交前校验、错误定位聚焦首字段
* 联动/级联：上级变→下级可选范围变（省→市→区）、级联清空不残留、联动后必填性变化
* 默认值与回显：新增默认值合理、编辑回显正确可改可存
* 计数与限制反馈：字数计数器实时、超限截断或禁提交
* 表单状态保护：提交中按钮禁用防重、校验错误不清空已填、成功后重置或跳转

**4. 操作按钮与交互动作**
* 按业务状态/权限/选中态启用禁用：未选行时"编辑"禁用、终态不可再操作、库存0禁用下单（锚定业务状态，非"按钮能否点"）
* 二次确认：危险操作（删除/支付/退款）弹确认框、确认/取消两分支结果不同
* 操作反馈：成功/失败/进行中反馈、操作后列表刷新与状态更新、失败不破坏当前数据
* 批量操作闭环：选中→操作→结果汇总→列表刷新

**5. 页面状态与反馈**
* 加载态：首屏骨架/Loading、异步区域占位、加载中不可重复触发
* 四态区分：无数据/无权限/查询无结果/加载失败，文案与重试入口差异化
* 异常反馈：网络/超时/服务端友好提示、重试入口、不暴露堆栈
* 消息提示：Toast/通知位置一致、自动消失、不阻断后续操作

**6. 导航与流程跳转**
* 跳转与回显：列表→详情→编辑→返回的面包屑、返回时查询条件与分页保持
* 未保存离开提示：表单脏数据离开弹确认、确认丢弃/取消继续
* 流程引导态：向导/多步当前步标识、可回退、步骤间数据保持、终步提交

**7. 权限驱动的 UI**（与权限类联动·高价值）
* 可见性：无权限菜单/按钮/页签隐藏还是禁用（按产品决策一致）、路由级无权限拦截
* 数据可见性与脱敏：列表只显本人/本组织数据、敏感字段按角色脱敏（手机号/身份证/金额）、脱敏前后端双校验
* 越权 UI 防护：直接输入无权限路由/参数的拦截（前端路由 + 后端双校验，前端不可单方面信任）

**8. 数据展示格式与极端值**（适度，与数据类正交的"展示面"）
* 格式一致性：日期/时间/货币/单位全局一致、按用户时区与语言偏好
* 枚举文案映射：状态码→文案全局一致、同一状态不同入口文案不冲突
* 极端值不破版：超长字符串/超大数值/负数/0/全特殊字符的展示不破版可读

#### UI 类边界表（防与其他维度重复·适度）

| 子维度 | 属 UI（界面承载） | 不属 UI（归别处，不重复测） |
| -- | -- | -- |
| 查询条件 | 组合态/默认态/重置/联动 | 单字段值校验（输入类）、查询接口契约（接口类） |
| 列表展示 | 字段映射/分页/排序/四态 | 列表数据正确性本身（数据类）、分页参数（接口类） |
| 表单 | 联动/回显/校验反馈/状态保护 | 字段值边界（边界类）、字段类型（输入类） |
| 按钮 | 按业务状态启用/二次确认/反馈 | 按钮点击物理可行性（禁测，0.1） |
| 权限 UI | 可见性/脱敏/路由拦截 | 越权访问后端拦截（安全类） |
| 数据展示 | 格式/映射/不破版 | 数据计算正确性（数据类） |

#### UI 类适度性校准

* **不测**（框架能力，0.1 守住）：宿主能否打开、页面能否渲染、按钮物理可点、滚动条能否滚、框架动画/过渡、纯元素存在性、像素级布局/配色/字体
* **测**（业务承载，必测）：字段映射/格式/精度、分页排序、四态区分、查询组合态、权限可见性与脱敏、按钮按业务态启用、二次确认与反馈——共同特征：Then 可断言"业务可见的数据/状态/权限变化"
* **按风险分级**（与 skill 风险体系一致，防爆炸）：P0/P1（资金/权限/状态流转/数据一致性）区→字段映射、脱敏、越权 UI 防护、终态按钮禁用全量；P2（常规业务流程/列表/查询）区→分页/排序/四态/组合查询/重置采样代表值；P3（低频边缘）区→极端值不破版、列宽响应式最小采样或跳过

---

# 15.6 用例生成端强制约束（防类型单一化·防漏测·第8阶段）

用例生成（第8阶段）须遵循以下生成端约束（与校验端 `scripts/verify_cases.py` 闭环）：

* **方法→列映射**：测试类型/测试维度列必须按 `references/methods.md` 决策表"方法→列映射"填写，禁止单一类型覆盖（全"功能测试"即不合格）。`verify_cases.py` 测试类型种类<3 提示方法未落地。
* **生成前覆盖自检**：生成前对照覆盖矩阵（`references/coverage.md` 8.x）+ 第5阶段风险清单，自检每个 P0/P1 风险有对应方法用例、每个高价值维度（安全/幂等/契约/并发/状态机非法流转）有覆盖；缺口在内存内补齐后再写入。
* **关联规则含 ID**：关联规则列须引用风险ID/测试点ID（见 20.3），实现风险/测试点→用例精确追溯。
* **禁止类型单一化**：全表单一测试类型判定不合格（见 SKILL.md 0.8）。

---

## 15.7 第8阶段出口机器 gate（强制·写前·内存内·有界自修）

> **根因**：原设计下 `verify_cases.py` 的客观机器检查**要到第13阶段（写后回读）才第一次跑**（`references/output_write.md` 第6步）。后果：第9去重/第10覆盖率/第11自查/第12展示全在**可能有机器可判缺陷的用例集**上做白工，写后才发现"测试类型<3/边界内=0/断言模糊/脑补行为"，回头重写整份 .md。本 gate 把机器检查前移到写前，使后续阶段作用在已通过机器校验的用例集上。

### 机制

第8阶段用例生成完、进入第9去重前，于**内存内**执行出口 gate（零文件操作，符合 `references/output_write.md`"落盘前零文件操作"铁律）：

1. **输入**：本轮将 Write 的**完整 .md 文本**（追溯性 section + 15列用例表）按行作为 `lines`——即 Write 的 `content` 本身；读取已强制落盘的 `case-design-out/REQ_<需求标识>.md` 为 `req_doc_lines` 一并传入（启用 #4 反向需求追溯 + #5 业务行为 token 核对，与 Phase 13 第2参数语义一致）。REQ_<需求标识>.md 已在第 0/1 阶段强制落盘（见 `references/phase0_manifest.md` 时机零、`references/output_write.md` REQ 落盘强制节），故 #4/#5 不再"皆无则跳过"；若该文件异常缺失（非正常路径），#4 降级为显式强提示"⚠ 需求条目级覆盖未校验：REQ_<需求标识>.md 缺失，须先落盘需求文档后重跑"（见 `references/dedup_coverage.md` #4 显式强提示），不静默放行
2. **调用**：调 `scripts/verify_cases.py` 的内存入口 `run_inmemory(lines, req_doc_lines=None)`（无需读源码，返回结构化 dict：hard_violations / soft 疑似 / coverage 统计 / traces 追溯）
3. **判定与自修**（有界循环，≤2 轮）：
   * `hard_violations` 非空（ID 跳号重复 / 测试类型·维度·等级越界 / 用例名称四段缺失 / 4 固定列错值）→ **内存内修**（确定性修复，如补序号、改正枚举、补固定列）→ 重跑本 gate
   * `soft` 疑似>0（断言可观测#4 / 存储合规#9 / 存储schema交叉#9增强 / 重复#6 / 过度设计#7 / 关联需求ID追溯 / 断言完整性#13 / 业务行为来源#5 / 规则来源#15）或 `coverage` 缺口（类型<3 / 高价值维度=0 / 状态机非法流转=0 / 边界4档缺 / 异常子类缺 / 规则·风险·测试点·接口追溯未覆盖）→ **内存内修**→重跑本 gate
   * **硬阻断**（机器判不了的"确无来源"）：杜撰存储（#9，需求未提供真实 schema 且命中杜撰正则）/ 业务行为确无来源（#5，行为信号不在需求文档且无 R/TP 引用且无假设标记）→ **不静默自修**，须转为待确认问题（P0/P1）或假设（P2/P3）登记入【待确认问题与假设清单】并回显给用户；其余可机器自修
4. **循环上限**：≤2 轮机器自修（机器可判项的修复是确定性的、置信度高，故上限小于 Phase 11 的 ≤3）；2 轮后仍有机器可判项未过 → 升级阻断，列入【测试设计缺口清单】停止（同 `references/review_gate.md` 门禁失败处理）
5. **通过**→进入第9阶段（去重）；此时用例集已通过机器校验

### 与第11阶段（写前 LLM 自查）的分工（防重叠循环·强制）

| 层 | 第8阶段出口 gate（本节） | 第11阶段 selfcheck |
| -- | -- | -- |
| 谁判 | `verify_cases.py` 客观 | LLM 主观 |
| 管什么 | 机器能判：4 断言可观测/6 重复/7 过度设计/9 存储/13 断言完整性/15 规则来源 + 覆盖统计 + 规则·风险·测试点·接口·需求追溯 | 机器判不了：5 数据一致性/10 自动化友好/11 AI可解析主观面/14 对抗生成遍 + 1/8/9/12/15 的阻断决策与转问题/假设 |
| 循环 | ≤2 轮（机器自修，独立计数） | ≤3 轮（主观+对抗，独立计数，见 `references/selfcheck.md`） |

* **不重叠**：第8 gate 修好后，第11对 4/6/7/13/15 客观面的复核降为"确认已过"（不触发冗余修复循环）；第11仍全量跑其主观面（5/10/11/14）与阻断决策
* **计数独立**：第8 gate 的 ≤2 轮与第11的 ≤3 轮**各自独立计数**，不共享、不嵌套，无无限循环风险

### 与第13阶段（写后回读）的分工（防重叠·强制）

* 第8 gate 在**内存内**跑全量 `run_inmemory`（写前）；第13在**落盘文件**上跑 `python verify_cases.py <file>`（写后）。亦可经 `scripts/run_phase.py gate8` / `run_phase.py readback` 包装器统一触发并留痕（sentinel + 状态机，见 SKILL.md §6.6）
* 第13**不缩范围**（仍全量）：因第11的检查14对抗生成遍**新增用例**可能引入新机器缺陷，第13回归必须能抓到
* 第13 soft-fail **不回触第8 gate 循环**：走现有"内存修+重新单次 Write 覆盖 ≤2次/文件"回归路径（`references/output_write.md` 第6步），受现有约束；第8 gate 通过 ≠ 第13 必过
* 方向性：第8（客观发现）→ 第11（主观+对抗）→ 第13（写后回归），非三遍 discovery

### 运行模式正交

* 完整模式：第8 gate 硬阻断；命中 P0/P1 硬阻断项停止转问题/假设
* 连跑/轻量模式：第8 gate 命中 P2/P3 → 假设登记放行（沿用 `SKILL.md 6.5` 自动放行机制），**gate 不放宽质量底线，只放宽"等人工"时点**；P0/P1 硬阻断仍停
* 重型/中型/轻型分级（`references/dedup_coverage.md` ch26）：第8 gate 全规模必跑（轻型不跳，与"自查11/审核14 不得跳过"一致）

### 设计约束（不可违背）

* `run_inmemory` 纯函数、零临时文件、零 Bash、零权限弹窗——与"内存内完成、落盘前零文件操作"哲学一致
* gate 输入 = Write 的 `content` 本身（同一份内存文本），不另造数据源
* 不新增 LLM 主观自判（复用已有机器信号，遵守 `references/review_gate.md`"声明≠核实"红线）
* **被检查14 critique 回跑**：第11阶段检查14对抗遍补出的新用例须回跑本第8 gate（因新增用例可能触发枚举越界/重复/断言不可观测/来源缺失等机器缺陷）；此回跑计入第8 gate 的 ≤2 轮上限，若耗尽按升级机制处理，不回触检查14（见 `references/selfcheck.md` 检查14 衔接）

---

# 20、字段规范（强制）

> 本章为测试用例字段规范的**唯一权威**，Markdown 与 Excel **共同遵循**。字段顺序、取值、固定值三者 .md 与 .xlsx 必须完全一致。

字段顺序固定如下（共 15 列，与 `references/excel.md` 21.4 一致）：

| 序 | 字段 | 说明 |
| -- | -- | -- |
| 1 | 用例ID | 见 20.1 |
| 2 | 关联需求ID | 见 20.2 |
| 3 | 关联规则 | 见 20.3 |
| 4 | 测试类型 | 见 20.4 |
| 5 | 测试维度 | 见 20.11 |
| 6 | 所属模块 | 本用例所属业务模块 |
| 7 | 用例名称 | 见 20.5 |
| 8 | Given | 前置条件（见 13.1） |
| 9 | When | 操作步骤（见 13.2） |
| 10 | Then | 可观测预期（见 13.3、quality_rules.md 11.2） |
| 11 | 编辑模式 | 固定值 STEP（见 20.7） |
| 12 | 标签 | 固定值 AI（见 20.8） |
| 13 | 责任人 | 固定值 AI（见 20.9） |
| 14 | 用例等级 | 见 20.6 |
| 15 | 用例状态 | 固定值 Completed（见 20.10） |

> 列序 1-10 为业务字段（逐条填写），列序 11-13、15 为固定值列（取值不变），列序 14 用例等级为风险标度（取 P0-P3，见 20.6）。Markdown 表格与 Excel 表头须严格按此顺序，禁止调换、禁止增删列。

> **输出形态（强制·双向锚定 `references/output_write.md`「最终测试用例格式」）**：全部测试用例必须输出为**单一 15 列 Markdown 管道表**（表头 + 分隔行 + 每条用例一物理行）；禁止列表/`**Given:**`块/每条单独小节等非表格形式，单元格内禁止真实换行（见 §13.2、§13.3 换行符约定）。

## 20.1 用例ID规范

格式：`<需求标识>_<功能缩写>_<序号>`

需求标识来自用户输入或索引文件匹配。功能缩写用于区分不同测试场景，但不拆分为多个文件（所有用例写入同一文件）。

示例：
* 订单创建-20260702_CREATE_001（需求标识="订单创建-20260702"，创建订单功能）
* 订单创建-20260702_PAY_001（订单支付功能）
* 支付重构_REFUND_002（需求标识="支付重构"，退款功能）

要求：全局唯一、同一需求标识+功能连续编号、不允许重复编号、不允许跳号。

## 20.2 关联需求ID规范

填写本用例对应的需求条目编号。
* 若需求文档提供编号：直接填写（如 REQ-ORD-001）
* 若未提供编号：填写"见需求文档<章节号或具体章节名>"（如 `见需求文档3.2订单创建`、`见需求文档订单创建节`），**禁止杜撰**
* **禁止笼统占位**：不得填写 `见需求文档'需求内容'`、`见需求文档`、`无` 等无法定位具体需求条目的值；每条用例须能追溯到具体需求条目（不同用例可指向同一条目，但不得全员同一笼统占位）

> `scripts/verify_cases.py` 关联需求ID追溯检测：全员均为笼统占位或全员相同占位 → 报"需求条目级追溯失效"。需求→用例覆盖闭环依赖此列可机器追溯。
> **#4 token 匹配口径（对齐写法·v0.x）**：#4 反向需求追溯按 `_req_item_tokens` 切标题 token（英文/数字段 + 中文 2-gram 滑动窗，如标题"3.2 订单创建功能"→"订单"/"单创"/"创建"/"建功"/"功能"/"3.2" 等），任一 token 出现在"关联需求ID"列即判该需求条目已覆盖。故 20.2 推荐写法"见需求文档3.2订单创建"（含"订单"/"创建"/"3.2"）可命中标题"3.2 订单创建功能"。仍建议关联赛博写法尽量含标题核心 2-gram，提高命中精度。

## 20.3 关联规则规范

填写本用例验证的核心业务规则（自然语言短句），**并引用对应风险ID/测试点ID**（如 `R1:库存扣减、TP2:库存边界`），实现风险/测试点→用例的精确追溯。

示例：`R1:订单金额满减规则`、`TP3:支付幂等规则`、`R2:订单状态待支付→已支付流转`。

> ID 引用规则：用例关联规则列含 `R<序号>` 引用对应风险（见 `TestCases_*.md` 的"风险清单"section）、含 `TP<序号>` 引用对应测试点（见"测试点清单"section）。`scripts/verify_cases.py` 优先按 ID 精确追溯（R1 后非数字，避免 R10 误匹配 R1），未标注 ID 时降级为 token 兜底匹配（标注"疑似"）。

禁止杜撰规则；禁止留空。

## 20.4 测试类型规范

仅允许使用以下类型：

| 类型 | 说明 |
| ---- | ---------- |
| 功能 | 标准功能验证 |
| 边界 | 边界值验证 |
| 异常 | 异常场景验证 |
| 权限 | 权限控制验证 |
| 状态迁移 | 状态流转验证 |
| 并发 | 并发场景验证 |
| 幂等 | 幂等验证 |
| 契约 | 接口契约验证 |
| 集成 | 上下游集成验证 |
| 兼容性 | 兼容性验证 |
| 安全 | 安全风险验证 |
| 可靠性 | 重试、恢复、降级验证 |

禁止新增自定义类型。

## 20.5 用例名称规范

格式：`【模块】【功能】【场景，预期】`

示例：`【订单模块】【创建订单】【库存不足】【返回库存不足错误】`

要求：
* 四段必须齐全（模块/功能/场景/预期），禁止缺段、禁止空段
* 【模块】与"所属模块"列保持一致；【预期】须与 Then 列可观测断言呼应
* 禁止杜撰模块/功能；场景与预期须来自已确认需求或澄清台账，不得脑补
* 禁止使用模糊描述（如"测试成功""功能正常"），【预期】须具体可观测
* 【场景】与【预期】均需**简明扼要**：用最短语句表达核心场景与预期结果，避免冗长复述 Given/When/Then 细节。每个字段宜为一短语，不宜成句成段
  * 正确：【订单模块】【创建订单】【库存不足】【返回库存不足错误】
  * 错误（冗长）：【订单模块】【创建订单】【用户购物车有商品A001数量2但仓库实际库存仅剩1件时点击提交订单】【系统拒绝创建订单并返回库存不足错误码同时订单主存储中不产生任何订单记录且库存不扣减】

## 20.6 用例等级规范

仅允许 P0-P3，含义与风险优先级体系（`references/risk.md`）一致：

| 等级 | 说明 |
| -- | ------ |
| P0 | 核心业务风险 |
| P1 | 高风险 |
| P2 | 中风险 |
| P3 | 低风险 |

用例等级取自第5阶段风险分析产出的 P0-P3，禁止自行编造等级。Markdown 与 Excel 均须填写。

## 20.7 编辑模式
固定值：STEP。.md 与 .xlsx 均须填写，取值固定不变，禁止改动、禁止留空。

## 20.8 标签
固定值：AI。.md 与 .xlsx 均须填写，取值固定不变，禁止改动、禁止留空。

## 20.9 责任人
固定值：AI。.md 与 .xlsx 均须填写，取值固定不变，禁止改动、禁止留空。

## 20.10 用例状态
固定值：Completed。.md 与 .xlsx 均须填写，取值固定不变，禁止改动、禁止留空。

## 20.11 测试维度规范

> 单一事实源为 `config/validation_rules.json` 的 `valid_dims`（13 值，含 `界面验证`）。本节为散文重述，**以 JSON 为准**（见 SKILL.md 校验规则契约节优先级）。`verify_cases.py` 启动时从 JSON 加载，越界即 exit=1。

允许（13 值）：输入验证、边界验证、状态验证、权限验证、数据验证、接口验证、并发验证、幂等验证、兼容性验证、集成验证、风险验证、安全验证、**界面验证**（UI 类用例填本维度，见 15.5 UI 类；v0.5.0 新增，旧散文曾漏列，以 13 值为准）。

禁止使用模糊维度。