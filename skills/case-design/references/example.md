# 端到端范例（Worked Example）

> 本参考文件为端到端范例（原 ch28）的**唯一来源**。**仅当不确定用例格式/结构时读取**；已知格式时无需读取。以"订单创建"贯穿，锚定全流程歧义。仅示意结构，非真实业务规则。

---

## 输入（节选）

```
<<<需求文档开始>>>
用户在购物车点击"提交订单"，系统创建订单，状态为"待支付"，扣减库存，发送订单创建消息。
订单金额 = 商品单价 × 数量。数量需为 1-99 整数。
<<<需求文档结束>>>
```

## 待确认问题与假设清单（示例）

> 统一展示：待确认问题（Q，阻断）与假设（A，非阻断）同列一张清单向用户展示，假设不再仅沉于交付报告（闭合"假设未在问题列表展示"）。问题与假设共用 ID 空间。

| 问题/假设ID | 类型 | 问题描述 | 影响范围 | 风险等级 | 状态 | 建议回答角色 |
| ---- | -- | ---- | ---- | ---- | ---- | ---- |
| Q1 | 业务规则 | 是否允许重复提交订单（幂等） | 订单创建 | P1 | 待确认(阻断) | @开发 |
| Q2 | 数据规则 | 库存扣减时机：下单即扣 vs 支付后扣 | 库存、订单 | P0 | 待确认(阻断) | @开发 |
| Q3 | 异常规则 | 库存不足时是拒绝下单还是允许超卖 | 订单 | P0 | 待确认(阻断) | @产品 |
| A1 | 状态机 | 待支付后续状态（已支付/已发货/已收货）及逆向流转，需求文档未明确 | 订单 | P2 | 假设(非阻断,事后确认) | @开发 |

> 存在 P0 未确认 → 停止，等待用户回复。A1 为连跑/轻量模式下登记的假设（状态机需求未明确，须回显本清单，受影响用例关联规则标 `假设A1`），提示用户事后定向确认；完整模式下状态机属 P1 须阻断提问，不得静默假设。

## 测试用例（示例，假设澄清已完成）

> 字段为 15 列（与 `references/modeling.md`/`references/excel.md` 21.4 一致），用例等级位于用例状态之前；含 4 个固定值列（编辑模式=STEP、标签=AI、责任人=AI、用例状态=Completed）。
> 本范例用例ID采用简化形式展示（ORD_CREATE_xxx），实际应遵循 20.1 规范：`<需求标识>_<功能缩写>_<序号>`，如需求标识="订单创建-20260702" → `订单创建-20260702_CREATE_001`。

| 用例ID | 关联需求ID | 关联规则 | 测试类型 | 测试维度 | 所属模块 | 用例名称 | Given | When | Then | 编辑模式 | 标签 | 责任人 | 用例等级 | 用例状态 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- | ----- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| ORD_CREATE_001 | 见需求文档订单创建节 | 订单创建主流程 | 功能 | 接口验证 | 订单 | 【订单模块】【创建订单】【库存充足】【创建订单成功】 | 用户已登录；购物车有商品A001数量2；库存充足 | 1.进入购物车点击提交订单；2.确认金额 | 接口返回200且status=SUCCESS；订单主存储中订单状态为待支付；库存数量减少2；发送订单创建消息事件 | STEP | AI | AI | P0 | Completed |
| ORD_CREATE_002 | 见需求文档订单创建节 | 数量边界 | 边界 | 边界验证 | 订单 | 【订单模块】【创建订单】【数量边界1与99】【创建成功】 | 用户已登录；购物车有商品A001 | 1.数量设为1提交；2.数量设为99提交 | 接口返回200；订单创建成功 | STEP | AI | AI | P1 | Completed |
| ORD_CREATE_002B | 见需求文档订单创建节 | 数量边界内邻接 | 边界 | 边界验证 | 订单 | 【订单模块】【创建订单】【数量边界内2与98】【创建成功】 | 用户已登录；购物车有商品A001 | 1.数量设为2提交；2.数量设为98提交 | 接口返回200且status=SUCCESS；订单主存储中订单状态为待支付；库存数量减少对应值 | STEP | AI | AI | P1 | Completed |
| ORD_CREATE_003 | 见需求文档订单创建节 | 数量非法 | 异常 | 输入验证 | 订单 | 【订单模块】【创建订单】【数量0与100非法】【返回参数非法错误】 | 用户已登录 | 1.数量设为0提交；2.数量设为100提交 | 接口返回400且错误码=PARAM_INVALID；订单未创建；库存未扣减 | STEP | AI | AI | P2 | Completed |
| ORD_CREATE_004 | 见需求文档订单创建节 | 重复提交幂等 | 幂等 | 幂等验证 | 订单 | 【订单模块】【创建订单】【并发重复提交】【仅创建一个订单】 | 用户已登录；购物车有商品A001 | 1.同一请求连续提交两次 | 仅创建1个订单；库存仅扣减一次；无重复消息事件 | STEP | AI | AI | P1 | Completed |

> 说明：本范例遵守 `references/quality_rules.md` ch12，未杜撰表名/Key/Topic，仅用业务语义描述存储与消息。ORD_CREATE_002B 演示**边界值 4 值模型中的边界内邻接**（min+1=2 / max-1=98，刚好满足约束应通过），与边界点 1/99、边界外 0/100 区分，防 off-by-one 漏测（见 `references/methods.md` 边界值 4 值模型）。

## 输出与审核

生成 `case-design-out/订单创建-20260702.md`（一次性写入，包含订单创建所有模块用例）→ 提示人工审核 → 用户回复"审核通过" → 自动生成/更新 `case-design-out/Knowledge_订单创建-20260702.md`（`references/knowledge.md`）→ 询问是否生成 Excel → 用户确认 → 生成 `case-design-out/订单创建-20260702.xlsx`（字段与 .md 一致）。

> 说明：模块用例写入同一文件 `case-design-out/订单创建-20260702.md`，通过用例ID的功能缩写（CREATE/PAY等）区分；默认单文件 `case-design-out/TestCases_<需求标识>.md`，仅合并体压缩后仍超 24000 token 才拆最小 PART（见 `references/output_write.md` 写前规模评估·输出 token 预算）。

---

## 范例2：状态迁移（订单状态机）

> 锚定与"订单创建"不同的设计方法：状态迁移法。展示合法流转、非法流转、回滚的用例结构。**需求文档仅明确"待支付"状态，后续状态机未定义**--完整模式下须作为问题提问（P1 阻断）；本范例为演示用例结构，连跑/轻量模式下登记为**假设 A1**（待 @开发 确认，已回显进待确认问题与假设清单），假设状态机为 待支付 → 已支付 → 已发货 → 已收货，可逆向 待支付 → 已取消、已支付 → 已退款。受影响用例关联规则标 `假设A1`。

### 状态机（澄清后确认）
```text
待支付 →[支付成功]→ 已支付 →[发货]→ 已发货 →[确认收货]→ 已收货
待支付 →[取消]→ 已取消
已支付 →[退款]→ 已退款
已发货 →[退款退货]→ 已退款（P2 假设A1，待@业务确认）
非法：已收货 → 已支付（不可逆）、已取消 → 任意状态（终态）
```

### 规则建模（示例·来源标注）

- **订单状态机** [来源:假设A1]（待支付→已支付→已发货→已收货 及逆向流转，需求未明确，登记假设待@开发确认）
- **非法流转拦截** [来源:假设A1]（终态后操作/不可逆流转拦截）

> 每条规则项标注来源（需求文档/台账Q/假设A），破 rule_coverage 自证洗白（见 `references/modeling.md` 规则建模 section 来源标注、`scripts/verify_cases.py` 检查15 规则来源）。

### 测试用例（示例）

> 字段为 15 列（与 21.4 一致），用例等级位于用例状态之前；含 4 个固定值列。

| 用例ID | 关联需求ID | 关联规则 | 测试类型 | 测试维度 | 所属模块 | 用例名称 | Given | When | Then | 编辑模式 | 标签 | 责任人 | 用例等级 | 用例状态 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- | ----- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| ORD_STATE_001 | 见需求文档订单状态节 | 合法流转 待支付→已支付 假设A1 | 状态迁移 | 状态验证 | 订单 | 【订单模块】【状态流转】【待支付支付成功】【流转已支付】 | 订单状态为待支付 | 1.对订单执行支付成功 | 接口返回200；订单主存储中订单状态更新为已支付；触发后续发货就绪 | STEP | AI | AI | P0 | Completed |
| ORD_STATE_002 | 见需求文档订单状态节 | 合法流转 已支付→已发货 假设A1 | 状态迁移 | 状态验证 | 订单 | 【订单模块】【状态流转】【已支付发货】【流转已发货】 | 订单状态为已支付 | 1.执行发货 | 订单状态更新为已发货 | STEP | AI | AI | P1 | Completed |
| ORD_STATE_003 | 见需求文档订单状态节 | 逆向流转 待支付→已取消 假设A1 | 状态迁移 | 状态验证 | 订单 | 【订单模块】【状态流转】【待支付取消】【流转已取消】 | 订单状态为待支付 | 1.执行取消订单 | 订单状态更新为已取消；释放占用库存 | STEP | AI | AI | P1 | Completed |
| ORD_STATE_004 | 见需求文档订单状态节 | 非法流转 已收货→已支付 假设A1 | 状态迁移 | 状态验证 | 订单 | 【订单模块】【状态流转】【已收货逆向支付】【拦截并返回状态非法错误】 | 订单状态为已收货 | 1.尝试对已收货订单发起支付 | 接口返回400且错误码=INVALID_STATE_TRANSITION；订单状态保持已收货不变 | STEP | AI | AI | P1 | Completed |
| ORD_STATE_005 | 见需求文档订单状态节 | 非法流转 已取消→任意（终态） 假设A1 | 状态迁移 | 状态验证 | 订单 | 【订单模块】【状态流转】【已取消终态发货】【拦截并返回状态非法错误】 | 订单状态为已取消 | 1.尝试对已取消订单发货 | 接口返回400且错误码=INVALID_STATE_TRANSITION；订单状态保持已取消 | STEP | AI | AI | P1 | Completed |

> 说明：本范例展示状态迁移法的三类用例——合法正向流转、合法逆向/回滚流转、非法流转拦截。Then 强制断言状态变化 + 错误码，遵守 ch12。非法流转用例是状态机测试的价值核心，不可省略。

---

## 范例3：接口契约测试（契约驱动分支）

> 锚定"接口文档驱动"：输入含接口文档时启用契约驱动分支（第0阶段输入形态探测）。展示变更影响清单 + 统一接口测试矩阵三类用例（契约/规则/场景）。仅示意结构，非真实业务规则。

### 输入（节选）

```
<<<接口契约开始>>>
POST /order/create  v2.1：新增 couponId（string，选填）；amount（decimal，必填）。
<<<接口契约结束>>>
```

### 变更影响清单（接口契约模型）

| 接口ID | 接口名/路径 | 方法 | 变更类型 | 变更描述 | 变更字段 | 受影响规则 | 受影响场景 | 风险等级 | 来源 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| API1 | /order/create | POST | 新增字段 | 新增 couponId 选填 | couponId:string,选填 | R5:满减计算 | SC1:下单主流程 | P1 | [接口文档v2.1] |

> 每条标来源（破自证循环）；用例"关联规则"列引用 `API<序号>`/`R<序号>`/`SC<序号>` 实现精确追溯。

### 测试用例（契约/规则/场景三类）

> 字段为 15 列（与 `references/modeling.md`/`references/excel.md` 21.4 一致）。API1_001/002 为契约类（A 存在性 / B 类型），API1_003 为规则类（G）+ 场景类（H）。三类齐全方过 #6 反向接口追溯。

| 用例ID | 关联需求ID | 关联规则 | 测试类型 | 测试维度 | 所属模块 | 用例名称 | Given | When | Then | 编辑模式 | 标签 | 责任人 | 用例等级 | 用例状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ORD_API1_001 | 见需求文档订单创建节 | API1:couponId契约 | 异常 | 输入验证 | 订单 | 【订单】【创建订单】【couponId不传】【按选填放行】 | 用户已登录 | 1.couponId 不传提交 | 接口返回200（选填字段缺失放行）；订单状态为待支付 | STEP | AI | AI | P2 | Completed |
| ORD_API1_002 | 见需求文档订单创建节 | API1:couponId契约 | 异常 | 输入验证 | 订单 | 【订单】【创建订单】【couponId类型错传】【返回类型错误】 | 用户已登录 | 1.couponId 传数值类型 | 接口返回400且错误码=PARAM_TYPE_INVALID | STEP | AI | AI | P1 | Completed |
| ORD_API1_003 | 见需求文档订单创建节 | R5:满减、SC1:下单流程 | 功能 | 数据验证 | 订单 | 【订单】【创建订单】【带券满减】【满减计算正确】 | 用户已登录 | 1.couponId 传有效券下单 | 满减计算正确；订单主存储中订单状态为待支付 | STEP | AI | AI | P1 | Completed |

> 说明：本范例遵守 `references/quality_rules.md` ch12，未杜撰表名/Key/Topic，仅用业务语义描述存储。契约类（接/拒都对）全绿不等于安全--"券+积分叠加算错"属规则类 G3，须单测。
